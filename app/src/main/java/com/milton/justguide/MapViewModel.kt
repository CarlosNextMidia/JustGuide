package com.milton.justguide

import androidx.lifecycle.ViewModel

class MapViewModel : ViewModel() {
    // Nesta classe, vamos armazenar o estado do mapa, a lista de coordenadas
    // para desenhar a rota e a velocidade atual.

    // Por enquanto, apenas um placeholder para futuras funcionalidades:
    fun startTracking() {
        // Lógica de GPS em tempo real virá aqui
    }

    fun stopTracking() {
        // Lógica para parar o GPS e salvar dados virá aqui
    }
}